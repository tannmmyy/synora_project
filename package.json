{
  "name": "synora-docs",
  "version": "2.0.0",
  "private": true,
  "description": "Synora Docs - Real-Time Collaborative Document Suite with CRDT Engine, TipTap, WebSockets, Live Presence, and AI Assistant",
  "scripts": {
    "build": "npm --prefix server install && npm --prefix server run build && npm --prefix client install && npm --prefix client run build",
    "start": "node server/dist/server.js",
    "dev": "npm --prefix server run dev",
    "server:dev": "npm --prefix server run dev",
    "client:dev": "npm --prefix client run dev",
    "test:crdt": "npm --prefix server run test:crdt"
  },
  "keywords": [
    "crdt",
    "yjs",
    "collaborative-editor",
    "google-docs",
    "synora",
    "tiptap",
    "prosemirror",
    "websockets",
    "real-time"
  ],
  "author": "Tanmoy Ghosh",
  "license": "MIT"
}
